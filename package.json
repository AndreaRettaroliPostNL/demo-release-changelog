{
  "devDependencies": {
    "conventional-changelog-cli": "^4.1.0",
    "conventional-changelog-conventionalcommits": "^7.0.2"
  },
  "scripts": {
    "changelog:append": "conventional-changelog -p conventionalcommits -i CHANGELOG.md -s",
    "changelog:from": "node .github/scripts/changelog-from.js"
  }
}
